package com.example.calculator;

public enum InputEnum {
    // Operators
    MINUS, PLUS, MULTIPLY, DIVIDE,
    SQRT, PERCENT, CHANGE_SIGN,
    // Special keys
    CLEAR, EQUALS,
    // Decimal to be treated like a regular integer
    DECIMAL;
}
